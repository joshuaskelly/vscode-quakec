{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
            "name": "Run Tests",
            "stopOnEntry": false,
            "args": ["${workspaceRoot}/test/**/*.js", "--no-timeouts"],
            "cwd": "${workspaceRoot}"
        }
    ]
}